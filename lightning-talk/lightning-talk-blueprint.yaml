name: Lightning Talk App
services:
  - type: org.apache.brooklyn.entity.webapp.tomcat.Tomcat8Server
    name: Tomcat On Up
    brooklyn.config:
      wars.root: 'https://tomcat.apache.org/tomcat-8.0-doc/appdev/sample/sample.war'
    location: Up
    brooklyn.enrichers:
      - type: io.cloudsoft.amp.ui.common.enricher.AmpExternalUiModule
        brooklyn.config:
          external.ui.module.name: Tomcat UI on Up
          external.ui.module.icon: fa-snapchat-ghost
  - type: org.apache.brooklyn.entity.webapp.tomcat.Tomcat8Server
    name: Tomcat On Down
    brooklyn.config:
      wars.root: 'https://tomcat.apache.org/tomcat-8.0-doc/appdev/sample/sample.war'
    location: Down
    brooklyn.enrichers:
      - type: io.cloudsoft.amp.ui.common.enricher.AmpExternalUiModule
        brooklyn.config:
          external.ui.module.name: Tomcat UI on Down
          external.ui.module.icon: fa-github
  - type: org.apache.brooklyn.entity.webapp.tomcat.Tomcat8Server
    name: Tomcat On Charm
    brooklyn.config:
      wars.root: 'https://tomcat.apache.org/tomcat-8.0-doc/appdev/sample/sample.war'
    location: Charm
    brooklyn.enrichers:
      - type: io.cloudsoft.amp.ui.common.enricher.AmpExternalUiModule
        brooklyn.config:
          external.ui.module.name: Tomcat UI on Charm
          external.ui.module.icon: fa-reddit
  - type: org.apache.brooklyn.entity.webapp.tomcat.Tomcat8Server
    name: Tomcat On Strange
    brooklyn.config:
      wars.root: 'https://tomcat.apache.org/tomcat-8.0-doc/appdev/sample/sample.war'
    location: Strange
    brooklyn.enrichers:
      - type: io.cloudsoft.amp.ui.common.enricher.AmpExternalUiModule
        brooklyn.config:
          external.ui.module.name: Tomcat UI on Strange
          external.ui.module.icon: fa-steam
  - type: org.apache.brooklyn.entity.webapp.tomcat.Tomcat8Server
    name: Tomcat On Top
    brooklyn.config:
      wars.root: 'https://tomcat.apache.org/tomcat-8.0-doc/appdev/sample/sample.war'
    location: Top
    brooklyn.enrichers:
      - type: io.cloudsoft.amp.ui.common.enricher.AmpExternalUiModule
        brooklyn.config:
          external.ui.module.name: Tomcat UI on Top
          external.ui.module.icon: fa-android
  - type: org.apache.brooklyn.entity.webapp.tomcat.Tomcat8Server
    name: Tomcat On Bottom
    brooklyn.config:
      wars.root: 'https://tomcat.apache.org/tomcat-8.0-doc/appdev/sample/sample.war'
    location: Bottom
    brooklyn.enrichers:
      - type: io.cloudsoft.amp.ui.common.enricher.AmpExternalUiModule
        brooklyn.config:
          external.ui.module.name: Tomcat UI on Bottom
          external.ui.module.icon: fa-slack
